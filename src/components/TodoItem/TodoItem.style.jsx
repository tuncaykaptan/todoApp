import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    todoItemContainer: {
        padding: 10,
        backgroundColor: '#40aa0f',
        borderRadius: 8,
        marginTop: 10,
      },
      todoItemText: {
        color: 'white',
      },
});

export default styles;
